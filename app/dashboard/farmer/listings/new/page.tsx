"use client";
import {useEffect,useState} from "react";import {useRouter} from "next/navigation";import {createClient} from "@/lib/supabase/client";
type Crop={id:string;crop_name:string;available_quantity_kg:number|null;quality_grade:string|null};
export default function AddProducePage(){const router=useRouter();const[crops,setCrops]=useState<Crop[]>([]);const[form,setForm]=useState({crop_id:"",quantity_kg:"",quality:"Grade A",location:"",available_until:""});const[error,setError]=useState<string|null>(null);const[saving,setSaving]=useState(false);useEffect(()=>{(async()=>{const s=createClient();const{data:{user}}=await s.auth.getUser();if(user){const{data}=await s.from("crops").select("id,crop_name,available_quantity_kg,quality_grade").eq("farm_id",(await s.from("farms").select("id").eq("owner_id",user.id).maybeSingle()).data?.id??"");setCrops(data??[])}})()},[]);async function submit(){const c=crops.find(x=>x.id===form.crop_id);if(!c||!form.quantity_kg){setError("Select a crop and quantity.");return}setSaving(true);setError(null);const s=createClient();const{data:{user}}=await s.auth.getUser();if(!user){setError("Not signed in.");setSaving(false);return}const{error:e}=await s.from("farmplug_supply_listings").insert({farmer_name:user.user_metadata?.full_name??user.email??"Farmer",crop:c.crop_name,quantity_kg:Number(form.quantity_kg),quality:form.quality,location:form.location||null,available_until:form.available_until||null,status:"published",created_by:user.id});if(e){setError("Couldn't publish listing.");setSaving(false);return}router.push("/dashboard/farmer/listings")};return <div className="mx-auto max-w-md p-6"><h1 className="text-xl font-semibold">Add Produce Listing</h1><div className="mt-4 space-y-3"><select value={form.crop_id} onChange={e=>setForm({...form,crop_id:e.target.value})} className="w-full rounded-md border px-3 py-2 text-sm"><option value="">Select crop</option>{crops.map(c=><option key={c.id} value={c.id}>{c.crop_name}</option>)}</select><input type="number" placeholder="Quantity (kg)" value={form.quantity_kg} onChange={e=>setForm({...form,quantity_kg:e.target.value})} className="w-full rounded-md border px-3 py-2 text-sm"/><select value={form.quality} onChange={e=>setForm({...form,quality:e.target.value})} className="w-full rounded-md border px-3 py-2 text-sm"><option>Grade A</option><option>Grade B</option><option>Grade C</option></select><input placeholder="Location" value={form.location} onChange={e=>setForm({...form,location:e.target.value})} className="w-full rounded-md border px-3 py-2 text-sm"/><label className="block text-xs">Available until</label><input type="date" value={form.available_until} onChange={e=>setForm({...form,available_until:e.target.value})} className="w-full rounded-md border px-3 py-2 text-sm"/></div>{error&&<p className="mt-3 text-sm text-[#B42318]">{error}</p>}<div className="mt-4 flex justify-end"><button disabled={saving} onClick={submit} className="rounded-md bg-[#1B4332] px-4 py-2 text-sm font-medium text-white disabled:opacity-50">{saving?"Publishing...":"Publish Listing"}</button></div></div>}
