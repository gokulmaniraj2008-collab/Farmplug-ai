"use client";import{useEffect,useState}from"react";import{createClient}from"@/lib/supabase/client";export default function Page(){const[d,setD]=useState<any[]>([]);useEffect(()=>{(async()=>{const{data}=await createClient().from("farmplug_buyer_requirements").select("id,buyer_name,crop,quantity_kg,quality,location,delivery_days,status,is_verified,created_at").order("created_at",{ascending:false});setD(data??[])})()},[]);return <main className="p-6"><h1 className="text-2xl font-bold">Buyer requirements</h1><div className="mt-5 space-y-2">{d.map(x=><div className="rounded-xl border p-4" key={x.id}><div className="flex justify-between"><b>{x.crop} · {x.quantity_kg} kg</b><span>{x.status}</span></div><p className="text-sm text-gray-500">{x.buyer_name} · {x.quality} · {x.location} · {x.delivery_days} days · {x.is_verified?"verified":"unverified"}</p></div>)}</div></main>}