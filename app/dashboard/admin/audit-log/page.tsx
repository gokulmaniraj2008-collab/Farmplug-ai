"use client";
import {useEffect,useState} from "react";import{createClient}from"@/lib/supabase/client";
export default function AdminAuditLogPage(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{(async()=>{const{data}=await createClient().from("audit_logs").select("id,actor_id,action,entity_type,entity_id,metadata,created_at").order("created_at",{ascending:false}).limit(100);setRows(data??[])})()},[]);return <main className="mx-auto max-w-5xl p-6"><h1 className="text-2xl font-bold">Audit log</h1><p className="mt-1 text-sm text-gray-500">Latest 100 platform events.</p><div className="mt-5 space-y-2">{rows.length?rows.map(r=><div key={r.id} className="rounded-xl border p-3"><div className="font-medium">{r.action} · {r.entity_type}</div><div className="text-xs text-gray-500">{r.entity_id??"—"} · {new Date(r.created_at).toLocaleString()}</div></div>):<p className="text-sm text-gray-500">No audit events recorded yet.</p>}</div></main>}
