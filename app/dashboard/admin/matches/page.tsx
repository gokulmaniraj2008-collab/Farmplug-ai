"use client";import{useEffect,useState}from"react";import{createClient}from"@/lib/supabase/client";export default function Page(){const[d,setD]=useState<any[]>([]);useEffect(()=>{(async()=>{const{data}=await createClient().from("buyer_matches").select("id,buyer_id,supply_listing_id,crop_score,quantity_score,quality_score,location_score,delivery_score,total_score,explanation,created_at").order("total_score",{ascending:false});setD(data??[])})()},[]);return <main className="p-6"><h1 className="text-2xl font-bold">Buyer matches</h1><div className="mt-5 space-y-2">{d.map(x=><div className="rounded-xl border p-4" key={x.id}><b>Match score: {x.total_score}/100</b><p className="text-xs text-gray-500">Crop {x.crop_score} · Qty {x.quantity_score} · Quality {x.quality_score} · Location {x.location_score} · Delivery {x.delivery_score}</p></div>)}{!d.length&&<p className="text-sm text-gray-500">No calculated matches yet.</p>}</div></main>}