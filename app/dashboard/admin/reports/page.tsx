"use client";import{useEffect,useState}from"react";import{createClient}from"@/lib/supabase/client";export default function Page(){const[s,setS]=useState<any>({});useEffect(()=>{(async()=>{const c=createClient();const[q,l,o,d]=await Promise.all([c.from("farmplug_buyer_requirements").select("id",{count:"exact",head:true}),c.from("farmplug_supply_listings").select("id",{count:"exact",head:true}),c.from("farmplug_orders").select("id",{count:"exact",head:true}),c.from("disputes").select("id",{count:"exact",head:true})]);setS({requirements:q.count??0,listings:l.count??0,orders:o.count??0,disputes:d.count??0})})()},[]);return <main className="p-6"><h1 className="text-2xl font-bold">Platform reports</h1><div className="mt-5 grid grid-cols-2 gap-4 md:grid-cols-4">{Object.entries(s).map(([k,v])=><div key={k} className="rounded-xl border p-4"><b className="text-2xl">{String(v)}</b><p className="text-sm capitalize text-gray-500">{k}</p></div>)}</div></main>}