"use client";import{useEffect,useState}from"react";import{createClient}from"@/lib/supabase/client";export default function Page(){const[p,setP]=useState<any>(null);useEffect(()=>{(async()=>{const s=createClient();const{data:{user}}=await s.auth.getUser();if(user){const{data}=await s.from("profiles").select("email,role,farm_role,profile_complete,phone,location_text,auth_provider").eq("id",user.id).maybeSingle();setP(data)}})()},[]);return <main className="mx-auto max-w-2xl p-6"><h1 className="text-2xl font-bold">Admin settings</h1><div className="mt-5 rounded-xl border p-5">{p?<dl className="space-y-3 text-sm"><div><dt className="text-gray-500">Account</dt><dd>{p.email}</dd></div><div><dt className="text-gray-500">Access</dt><dd>{p.role} / {p.farm_role}</dd></div><div><dt className="text-gray-500">Auth</dt><dd>{p.auth_provider}</dd></div><div><dt className="text-gray-500">Profile</dt><dd>{p.profile_complete?"Complete":"Incomplete"}</dd></div></dl>:<p>Loading…</p>}</div><p className="mt-4 text-xs text-gray-500">Sensitive role and platform configuration changes remain server-side only.</p></main>}