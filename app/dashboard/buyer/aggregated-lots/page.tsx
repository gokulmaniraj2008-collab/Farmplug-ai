"use client";import{useEffect,useState}from"react";import{createClient}from"@/lib/supabase/client";export default function Page(){const[d,setD]=useState<any[]>([]);useEffect(()=>{(async()=>{const{data}=await createClient().from("supply_aggregations").select("id,requirement_id,total_quantity_kg,status,selected_listings,logistics_cost,created_at").order("created_at",{ascending:false});setD(data??[])})()},[]);return <main className="p-6"><h1 className="text-2xl font-bold">Aggregated lots</h1><div className="mt-5 space-y-3">{d.map(x=><div className="rounded-xl border p-4" key={x.id}><div className="flex justify-between"><b>{x.total_quantity_kg} kg</b><span>{x.status}</span></div><p className="text-sm text-gray-500">Requirement: {x.requirement_id} · logistics ₹{x.logistics_cost??"—"}</p></div>)}{!d.length&&<p className="text-sm text-gray-500">No aggregated lots available.</p>}</div></main>}