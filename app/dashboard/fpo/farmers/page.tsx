"use client";import{useEffect,useState}from"react";import{createClient}from"@/lib/supabase/client";export default function Page(){const[d,setD]=useState<any[]>([]);useEffect(()=>{(async()=>{const{data}=await createClient().from("farmer_profiles").select("id,full_name,phone,location,verification_status").order("created_at",{ascending:false});setD(data??[])})()},[]);return <main className="p-6"><h1 className="text-2xl font-bold">Member farmers</h1><div className="mt-5 grid gap-3 md:grid-cols-2">{d.map(x=><div className="rounded-xl border p-4" key={x.id}><b>{x.full_name}</b><p className="text-sm text-gray-500">{x.location??"—"} · {x.verification_status}</p></div>)}</div>{!d.length&&<p className="mt-4 text-sm text-gray-500">No farmer profile records yet.</p>}</main>}