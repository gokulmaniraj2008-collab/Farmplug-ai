"use client";import{useEffect,useState}from"react";import{createClient}from"@/lib/supabase/client";export default function Page(){const[d,setD]=useState<any[]>([]);useEffect(()=>{(async()=>{const{data}=await createClient().from("logistics_routes").select("id,collection_hub,buyer_location,status,estimated_cost,created_at").not("collection_hub","is",null).order("created_at",{ascending:false});setD(data??[])})()},[]);return <main className="p-6"><h1 className="text-2xl font-bold">Collection centers</h1><p className="mt-1 text-sm text-gray-500">Collection hubs currently referenced by logistics routes.</p><div className="mt-5 space-y-2">{d.map(x=><div className="rounded-xl border p-4" key={x.id}><b>{x.collection_hub}</b><p className="text-sm text-gray-500">Destination: {x.buyer_location} · {x.status}</p></div>)}</div>{!d.length&&<p className="mt-4 text-sm text-gray-500">No collection hubs recorded yet.</p>}</main>}