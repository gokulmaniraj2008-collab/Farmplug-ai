'use client';
import { useEffect, useState } from 'react';
import Link from 'next/link';
import { ArrowLeft, CheckCircle2, Clock3, Leaf, PackageCheck, Truck } from 'lucide-react';
import { supabase } from '@/lib/supabase';
type Order={id:string;status:string;quantity_kg:number;delivery_location:string;created_at:string;updated_at:string};
const nextFor=(status:string)=>({confirmed:'collecting',collecting:'in_transit',in_transit:'delivered',delivered:'completed'} as Record<string,string>)[status];
export default function OrdersPage(){const[orders,setOrders]=useState<Order[]>([]);const[loading,setLoading]=useState(true);const[message,setMessage]=useState('');const load=async()=>{if(!supabase){setLoading(false);setMessage('Supabase Auth is not configured.');return;}const{data:{session}}=await supabase.auth.getSession();if(!session){setLoading(false);setMessage('Sign in to view your orders.');return;}const r=await fetch('/api/orders',{headers:{Authorization:`Bearer ${session.access_token}`},cache:'no-store'});const j=await r.json();if(!r.ok)setMessage(j.error||'Could not load orders.');else setOrders(j.orders||[]);setLoading(false)};useEffect(()=>{load()},[]);const update=async(id:string,status:string)=>{if(!supabase)return;const{data:{session}}=await supabase.auth.getSession();if(!session)return;setMessage('Updating order…');const r=await fetch('/api/orders',{method:'PATCH',headers:{Authorization:`Bearer ${session.access_token}`,'Content-Type':'application/json'},body:JSON.stringify({id,status})});const j=await r.json();if(!r.ok){setMessage(j.error||'Order update failed.');return;}setMessage(`Order moved to ${status.replace('_',' ')}.`);await load()};return <main className="ordersPage"><header><Link href="/" className="back"><ArrowLeft size={17}/> FarmPlug AI</Link><span className="eyebrow"><PackageCheck size={14}/> FULFILMENT</span><h1>Orders & fulfilment</h1><p>Every accepted quote becomes a trackable FarmPlug order.</p></header><section className="wrap">{message&&<div className="message">{message}</div>}{loading?<div className="card">Loading live orders…</div>:orders.length===0?<div className="card empty"><Clock3 size={24}/><h2>No orders yet</h2><p>Request a quote from a live supply listing, then accept it as the farmer/FPO to create the first order.</p><div><Link href="/buyer">Open Buyer Center</Link><Link href="/dashboard">Open Farmer Dashboard</Link></div></div>:<div className="stack">{orders.map(order=><article className="card" key={order.id}><div className="top"><div><span className="eyebrow">ORDER #{order.id.slice(0,8)}</span><h2>{Number(order.quantity_kg).toLocaleString()} kg</h2><p>Delivery: {order.delivery_location}</p></div><b className={`status ${order.status}`}>{order.status.replace('_',' ')}</b></div><div className="timeline"><span className={['confirmed','collecting','in_transit','delivered','completed'].includes(order.status)?'done':''}>Confirmed</span><span className={['collecting','in_transit','delivered','completed'].includes(order.status)?'done':''}>Collecting</span><span className={['in_transit','delivered','completed'].includes(order.status)?'done':''}>In transit</span><span className={['delivered','completed'].includes(order.status)?'done':''}>Delivered</span><span className={order.status==='completed'?'done':''}>Completed</span></div>{nextFor(order.status)&&<button className="advance" onClick={()=>update(order.id,nextFor(order.status))}>{nextFor(order.status)==='collecting'?<Leaf size={15}/>:nextFor(order.status)==='in_transit'?<Truck size={15}/>:<CheckCircle2 size={15}/>} Move to {nextFor(order.status).replace('_',' ')}</button>}</article>)}</div>}<div className="note"><b>Transaction scope</b><span>FarmPlug records the commercial workflow; no payment gateway or financial settlement is claimed yet.</span></div></section><style jsx>{`.ordersPage{min-height:100vh;background:#f5f9f5;color:#17331f}.ordersPage>header{background:#173d25;color:#fff;padding:28px max(18px,calc((100% - 1000px)/2)) 40px}.back{display:inline-flex;gap:7px;align-items:center;color:#d7e8da;text-decoration:none;font-weight:800;margin-bottom:28px}.eyebrow{display:flex;gap:6px;align-items:center;font-size:10px;letter-spacing:.12em;font-weight:900;color:#718277}.ordersPage header .eyebrow{color:#cfe2d2}.ordersPage h1{font:800 44px 'Space Grotesk',sans-serif;margin:10px 0}.ordersPage header p{color:#d3e3d6}.wrap{max-width:1000px;margin:-12px auto 0;padding:0 18px 50px}.message,.note{padding:11px 13px;border-radius:11px;background:#fff8e8;border:1px solid #ebdcaf;color:#705b2b;font-size:12px;margin-bottom:14px}.stack{display:grid;gap:12px}.card{background:#fff;border:1px solid #dce8de;border-radius:17px;padding:19px;box-shadow:0 8px 24px rgba(23,51,31,.05)}.top{display:flex;justify-content:space-between;gap:15px}.card h2{font:800 24px 'Space Grotesk',sans-serif;margin:7px 0}.card p{font-size:12px;color:#6b7a70}.status{padding:7px 10px;border-radius:999px;font-size:10px;text-transform:uppercase;height:max-content}.status.confirmed,.status.collecting{background:#fff2d8;color:#805c10}.status.in_transit{background:#e8f1ff;color:#27547f}.status.delivered,.status.completed{background:#e4f5e6;color:#176329}.timeline{display:grid;grid-template-columns:repeat(5,1fr);gap:6px;margin:18px 0}.timeline span{background:#edf1ed;color:#8a968d;border-radius:8px;padding:8px 5px;text-align:center;font-size:9px;font-weight:800}.timeline span.done{background:#e4f5e6;color:#28733b}.advance{display:inline-flex;align-items:center;gap:6px;border:0;background:#28733b;color:#fff;border-radius:10px;padding:10px 13px;font-size:11px;font-weight:800;cursor:pointer}.empty{text-align:center;padding:40px 20px}.empty h2{margin-bottom:5px}.empty div{display:flex;justify-content:center;gap:9px;margin-top:16px}.empty a{border:1px solid #cbdacc;border-radius:9px;padding:9px 11px;color:#28733b;text-decoration:none;font-size:11px;font-weight:800}.note{margin-top:14px;background:#eff8f0;border-color:#d2e8d5;color:#506858}.note span{display:block;margin-top:4px}@media(max-width:700px){.ordersPage h1{font-size:35px}.top{display:block}.status{display:inline-block;margin-top:10px}.timeline{grid-template-columns:1fr 1fr}.empty div{flex-direction:column}}`}</style></main>}
