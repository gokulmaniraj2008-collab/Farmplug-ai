'use client';
import { FormEvent,useState } from 'react';
import { Mail,Send } from 'lucide-react';
export default function ContactPage(){const [sent,setSent]=useState(false);function submit(e:FormEvent){e.preventDefault();setSent(true)}return <main className="pageShell"><section className="pageHero"><span className="eyebrow"><Mail size={14}/> SUPPORT</span><h1>Contact FarmPlug AI.</h1><p>Tell us what you need help with. This prototype contact form confirms submission locally; connect it to your support backend before production.</p></section><section className="pageCard"><form onSubmit={submit}><div className="field"><label htmlFor="name">Name</label><input id="name" required/></div><div className="field"><label htmlFor="email">Email</label><input id="email" type="email" required/></div><div className="field"><label htmlFor="message">Message</label><textarea id="message" required rows={5} style={{width:'100%',border:'1px solid #d9e4dc',borderRadius:10,padding:11}}/></div>{sent&&<div role="status" className="notice">Message captured for this prototype. Connect the form to a support service for production delivery.</div>}<button className="btn primary" type="submit"><Send size={16}/> Send message</button></form></section></main>}
